{
  "name": "pain-server",
  "version": "0.1.0",
  "description": "Server of the PPP-Map",
  "license": "ISC",
  "author": "Michael Artner",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node --env-file=../pain-setup/db-config.env --env-file=./server.env dist/index.js",
    "dev": "npm run build && DEV=true npm run start",
    "dev-win": "npm run build && set DEV=true && npm run start",
    "test": "vitest run"
  },
  "dependencies": {
    "@types/supertest": "^7.2.0",
    "express": "^5.2.1",
    "pg": "^8.20.0",
    "pino": "^10.3.1",
    "supertest": "^7.2.2",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.0",
    "@types/pg": "^8.20.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
